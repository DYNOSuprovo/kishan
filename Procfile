# Build Command
# Render uses this file to determine how to build and run the application

# Install dependencies
pip install -r requirements.txt

# Run the application
web: gunicorn src.app:app --bind 0.0.0.0:$PORT